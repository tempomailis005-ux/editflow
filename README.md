# EditFlow - Freelance Editor Dashboard

## Local Development
Open `index.html` in browser or run:
```
npx -y http-server ./ -p 8080
```

## Deploy
1. Export data from admin panel (click "Export Data" button)
2. Save `data.json` to this folder
3. `git add . && git commit -m "update" && git push`
